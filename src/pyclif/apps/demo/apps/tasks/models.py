"""Data models for the Tasks app."""

from pyclif import BaseModel


class Tasks(BaseModel):
    """Domain model for the Tasks app."""

    # Declare your fields here, e.g.:
    # id: int
    # name: str
    # status: str
